name: ci-old-gcc

on: [push]

jobs:
  gcc-4:
    runs-on: ubuntu-latest
    container:
      image: ubuntu:trusty
#      image: library/gcc:4
    steps:
    - uses: actions/checkout@v2
    - name: ci
      shell: bash
      run: |
        pwd
        ls -l
        (which lsb_release && lsb_release -a) || true
        cmake --version || pip install cmake || true
        gcc -v || true
        make --version || true
        git --version || true
